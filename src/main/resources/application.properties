spring.application.name=HdfcNetBankingSimulator
server.port=9292

spring.datasource.url=jdbc:mysql://localhost:3306/netbanking_simulator
spring.datasource.username=root
spring.datasource.password=vaishnavi

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Same app/port right now (see common/EpiConstants.java for the shared identity/checksum
# key), but these two are kept as real HTTP base URLs - Merchant and Gateway only ever
# talk to each other over RestTemplate, never via in-process method calls.
epi.gateway-base-url=http://localhost:9292

bank.callback-delay-millis=6000